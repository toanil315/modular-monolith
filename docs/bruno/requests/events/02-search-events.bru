meta {
  name: 02 - Search Events
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/events/search?page=1&size=10
}

headers {
  Accept: application/json
}

tests {
  test("status is 2xx", function() {
    expect(res.status).to.be.within(200, 299);
    expect(res.body).to.be.an("object");
  });
}
